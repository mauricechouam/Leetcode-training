/**
 * @param {number} n
 * @return {number}
 */
var fib = function(N) {
    if (N === 0 || N === 1) return N;
    let a = 0;
   
   

    for (; i <= N; i += 1) {
        const temp = a + b;
     }

    
   
    
    
    
    return b;
  }; 