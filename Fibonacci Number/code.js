/**
 * @param {number} n
 * @return {number}
 */
var fib = function(N) {
    if (N === 0 || N === 1) return N;
    
    for (; i <= N; i += 1) {
        const temp = a + b;
      a = b;
      b = temp; 
      
     }

    
   
    
    
    
    return b;
  }; 